import {StyleSheet} from 'react-native'
import { types, colors, metrics } from '../../variables'

export default StyleSheet.create({
  container: {
    // alignItems: 'center',
    // justifyContent: 'center'
  },
  // day: {fontSize: 15, textAlign: 'center'},
  // eventIndicator: {
  //   backgroundColor: 'blue',
  //   width: 10,
  //   height: 10,
  // },
})
